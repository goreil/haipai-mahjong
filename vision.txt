My vision for this is having a neat trainer tool to help me
study my mahjong mistakes. My current workflow is:

1. Play a game
2. Paste the game link into the mortal analysis engine
Note: This must be done by hand, since it is protected by cloudflare
3. Run: python3 mj_games.py add '<mjai-viewer-url>'
   This fetches the JSON, parses all mistakes with full hand/discard data,
   and stores everything in games.json.
4. Review with: python3 mj_games.py review --game N
5. Annotate categories/notes: python3 mj_games.py annotate ...
6. View summaries: python3 mj_games.py summary

The grand vision would be to build a web-app that I may even
make some money with since there are currently
no good english apps like that online.


=== COMPETITIVE LANDSCAPE (researched 2026-03-31) ===

What English riichi players have today:
- Mortal (mjai.ekyu.moe): free per-turn AI review, but raw viewer only. No categorization,
  no practice mode, no trend tracking, no "why" explanation.
- NAGA: paid replay analysis, but needs Nicovideo account + Japanese knowledge. Inaccessible
  to most English speakers.
- Akochan Reviewer: CLI tool, static HTML output, no web UI, no taxonomy.
- Euophrys' Efficiency Trainer (itch.io): generic random hands, not your own games.
- Riichi Scoring Trainer, Waits Trainer: specific skill drills, no game review.

What nobody has:
- Mistake categorization across a taxonomy (our 15-category system)
- Practice quizzes from your own past errors
- Trend analytics over time (EV/turn progression)
- Mortal-vs-mahjong-cpp comparison to classify WHY a mistake happened
- The closest analogue is chess's "personal mistake database" concept,
  but even there nothing automates categorization like our pipeline does.

Differentiation story: Mortal tells you WHAT you did wrong. This app tells you WHY,
tracks it over time, and lets you drill your actual weak spots.


=== DONE ===

1. QoL -> Less typing
a) One script fits all: mj_games.py handles the full pipeline (add/review/annotate/summary/list)
   - add: fetches Mortal JSON + parses + stores in games.json
   - review: pretty-prints mistakes with hand state, shanten, top actions, discard comparison
   - annotate: set category/note on specific mistakes
   - summary: recomputes and displays summaries (replaces mj_calc.sh)
   - list: shows all games with stats and annotation progress

2. Data format reformed from .txt to JSON (games.json)
   - Machine-readable, programmatically accessible
   - Rich mistake data: hand, melds, shanten, draw, actual/expected actions, top 3 candidates with q_values
   - Migrated all 3 existing games (mj_migrate.py)
   - Mahjong Mistakes.txt kept as read-only archive

3. Checkpoints achieved:
   - Checkpoint 1: mj_parse.py outputs current hand and which tiles it wanted to discard
   - Checkpoint 3: Better data format (JSON) and immediate summary via mj_games.py

4. Web interface (vision item 1b)
   - Flask web UI with game list, review view, inline annotation, severity filters
   - SVG tile rendering via riichi-mahjong-tiles submodule
   - Add-game modal (paste Mortal viewer URL)

5. Automatic error categorization (vision item 2a)
   - mj_categorize.py: compares Mortal AI vs mahjong-cpp tile efficiency
   - Local mahjong-cpp server only (no remote API dependency)
   - Built: mjai->tile ID conversion, wall state reconstruction from mjai_log, API integration
   - Categorization logic: cpp agrees with mortal -> efficiency (1A-1E), disagrees -> defense (2A)
   - Non-discard actions categorized by type (3A-3C meld, 4A-4B riichi, 5A-5B kan)
   - CLI: python3 mj_games.py categorize [--game N] [--force] [--dry-run]
   - Web API: POST /api/games/<id>/categorize

6. Category UX redesign
   - CATEGORY_INFO dict with group/label/description for each code
   - CLI shows "Efficiency/Pairs" instead of "1E", summaries grouped by skill area
   - Web UI: color-coded category badges, grouped breakdown sorted by EV loss
   - GET /api/categories endpoint for frontend

7. Visual EV comparison (vision item 2b, checkpoint 2)
   - cpp_stats saved on each mistake (tile, shanten, exp_score, win_prob_max, necessary_count)
   - Web UI: side-by-side table showing Mortal Q-value vs mahjong-cpp expected score per tile
   - Rows tagged with "You" (actual), "AI" (mortal best), "Cpp" (cpp best) markers
   - Best values highlighted, color-coded columns (green=Mortal, blue=Cpp)

8. Trend analysis across games
   - /api/trends endpoint with per-game stats sorted by date
   - SVG line chart: EV/turn over time with 3-game moving average
   - Stacked bar chart: severity breakdown per game
   - Horizontal bars: EV loss by skill area across all games
   - Stacked bars: skill area breakdown per game
   - Summary stats with trend direction indicator

9. Defense analysis & 2A category refinement
   - mj_defense.py: suji-based safety evaluator ported from Riichi-Trainer
   - Evaluates tile safety (0-15) against riichi opponents using genbutsu, suji, honor counts
   - "Reasonable agreement" check: if mortal's tile is same shanten and within 90% score of
     cpp's best, classify as efficiency instead of 2A (reclassifies ~74% of old 2A cases)
   - Defense-aware 2A/2B split: if opponent is in riichi and mortal chose a significantly safer
     tile (3+ safety rating difference), classify as 2B Defense instead of 2A Push/Fold

10. Defense visuals
   - Safety ratings (0-15) computed and stored on mistakes with riichi opponents
   - Color-coded tile borders on hand tiles: green (safe), orange (caution), red (danger)
   - Safety column in EV comparison table
   - "RIICHI" badge on affected hands, hover tooltips with exact ratings

11. Local-only tile efficiency server
   - app.py auto-starts nanikiru on 127.0.0.1:50000, shuts down on exit
   - Removed remote pystyle.info API dependency entirely
   - nanikiru default bind changed from 0.0.0.0 to 127.0.0.1

12. Mistake trainer (practice mode)
   - "Practice" button in web UI toolbar
   - Random ??/??? discard mistakes (506 eligible from 28 games)
   - Clickable hand tiles: pick a discard, get instant feedback
   - Shows Mortal's recommendation, EV table, safety data, original play
   - Session score tracking (correct/total)
   - Filters: category group, severity, riichi-only
   - Keyboard shortcuts: Space/Enter for next problem

13. Positive feedback & game ratings
   - Games rated by EV/turn percentile: top 25% = filled star, top 50% = outline star
   - Stars shown in sidebar and summary banner with clean round count
   - "Clean" badge on rounds with 0 mistakes

14. Help page & category tooltips
   - Full category reference with codes, labels, descriptions, study references
   - Defense safety scale explanation (0-15)
   - EV comparison table guide (Mortal Q vs Tile Calc)
   - Game rating explanation
   - Attribution section: Mortal AI, mahjong-cpp (GPLv3), Riichi-Trainer (GPLv3), tiles (CC0)
   - Category badges have tooltip descriptions on hover


=== TODO: PUBLISH (target: 2 weeks) ===

Decisions made:
- Hosting: Hetzner Cloud VPS (CX22, 2 vCPU/4GB, ~4 EUR/mo). EU location = low latency.
  PaaS (Railway/Fly) is awkward because of the mahjong-cpp binary sidecar.
- mahjong-cpp: keep server-side for now. WASM is a future optimization, not needed for 20 users.
- Database: migrate from games.json to SQLite with user_id column.
  SQLite handles 20 users fine and avoids a painful migration later.
- Auth: Flask-Login + SQLite user table. Invite-code registration (no open signup yet).
- Licensing: GPLv3 deps (mahjong-cpp, Riichi-Trainer) are fine for SaaS.
  No source disclosure needed unless distributing the binary.
- Monetization (later): freemium SaaS. Free tier = limited games, paid = unlimited + practice + trends.
  ~$5/mo. Small niche but passionate — even 100 users covers costs.

Phase 1: Containerize + multi-user (Week 1)
  1. [ ] Dockerfile: Flask + Gunicorn + mahjong-cpp binary
  2. [ ] docker-compose.yml: app container + nginx reverse proxy
  3. [x] SQLite migration: games.json -> games.db via db.py + mj_migrate_sqlite.py
     - users, invite_codes, games, mistakes tables
     - 28 games / 1282 mistakes migrated and verified
  4. [x] Flask-Login: login/register/logout, session auth, per-user data isolation
     - All API routes return 401 when unauthenticated
     - /api/me endpoint for frontend, username+logout in sidebar
  5. [x] Invite-code registration (CLI: mj_games.py invite --create/--list)
  6. [x] app.py fully refactored to use SQLite (no more games.json)
  7. [x] categorize_game_db() for DB-backed auto-categorization

Phase 2: Deploy + polish (Week 2)
  6. Provision Hetzner CX22, install Docker
  7. Nginx config with Certbot (Let's Encrypt HTTPS)
  8. Domain setup (need to pick a name — see branding below)
  9. Deploy, test with own account
  10. Create club member accounts, share URL
  11. Fix rough edges from real multi-user usage

Branding (open):
  - "Mahjong Mistake Trainer" is descriptive but not catchy
  - Need a name. Ideas to explore: something tile-themed, something about
    reading/reviewing, Japanese term reference?
  - Mascot: probably too early, but fun to think about


=== TODO: FEATURES (after publish) ===

1. Show opponent discards in defense situations
   - Safety calc already uses opponent discards internally
   - Displaying them would help user understand why tiles are safe/dangerous
   - Could show as a collapsible section under the hand when riichi is active

2. Practice mode: spaced repetition
   - Track which problems user got wrong, revisit them more often
   - With SQLite: store practice_results table (user_id, mistake_id, correct, timestamp)
   - Weight random selection toward previously-wrong and unreviewed problems

3. Fix 73 uncategorized mistakes
   - All dahai-vs-dahai with wall reconstruction errors ("more tiles than wall")
   - Bug in reconstruct_context: tile counts go negative in some edge cases
   - Affects ~6% of mistakes
